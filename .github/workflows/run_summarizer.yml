name: Daily Freebie Summarizer

on:
  workflow_dispatch:
  schedule:
    # Runs once a day at 22:00 UTC
    - cron: '0 22 * * *'

jobs:
  run-summarizer-bot:
    runs-on: ubuntu-latest
    permissions:
      # This permission is essential to allow the Action to push code
      contents: write
    steps:
      - name: Check out repository code
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run the Summarizer bot
        env:
          GITHUB_ACTIONS: 'true'
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python bot_summarizer.py

      - name: Commit cleared daily deals log
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          # Stage any tracked-file changes safely, including an emptied daily_deals.txt
          git add -A daily_deals.txt
          # Check if the log reset was staged before committing
          if ! git diff --staged --quiet; then
            git commit -m "Clear daily deals log after summary"
            git push
          else
            echo "Daily deals log was already empty. No changes to commit."
          fi
