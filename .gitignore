# --- Legacy Python bot (retired; kept locally as backup, not tracked) ---
venv/
__pycache__/
*.pyc
parsers/
config.ini          # legacy plaintext secrets — rotate, do not commit
freebies.db          # legacy SQLite state — superseded by Neon
daily_deals.txt      # legacy summary queue — superseded by DB digests

# --- IDE / OS ---
.idea/
.vscode/
.DS_Store

# Note: the active app lives in web/ and has its own web/.gitignore
